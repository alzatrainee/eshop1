@using System.Collections.Generic
@using Microsoft.AspNetCore.Http
@using Microsoft.AspNetCore.Http.Authentication
@model LoginViewModel
@inject SignInManager<ApplicationUser> SignInManager

<div class="header" id="login-video">
    <video playsinline autoplay muted loop poster="~/videos/chick.jpg" id="bgvid">
        <source src="~/videos/stretching.mp4" type="video/mp4">
    </video>

<div class="container" style="margin-top: 250px; margin-bottom: 150px;">
    <div class="row">
        <div class="col-xs-10 col-xs-offset-1 col-sm-8 col-sm-offset-2 col-md-6 col-md-offset-3 col-lg-4 col-lg-offset-10">
            <div class="card card-signup custom-box-shadow">
                    <form asp-controller="Account" asp-action="Login" asp-route-returnurl="@ViewData["ReturnUrl"]" method="post" class="form">
                        <div class="header text-center custom-header">
                            <h4>Log In</h4>
                            <div class="social-line">
                                <a href="#pablo" class="btn btn-simple btn-just-icon">
                                    <i class="fa fa-vk"></i>
                                </a>
                                <a href="#pablo" class="btn btn-simple btn-just-icon">
                                    <i class="fa fa-facebook-square"></i>
                                </a>
                                <a href="#pablo" class="btn btn-simple btn-just-icon">
                                    <i class="fa fa-google-plus"></i>
                                </a>
                            </div>
                        </div>
                        <p class="text-divider">Or be Classical</p>
                        <div class="content">
                            <div asp-validation-summary="All" class="text-danger"></div>
                            <div class="input-group">
                                <label asp-for="Email" class="control-label input-group-addon">
                                    <i class="material-icons">email</i>
                                </label>
                                <input asp-for="Email" class="form-control" placeholder="E-Mail" />
                                <span asp-validation-for="Email" class="text-danger"></span>
                            </div>
                            <div class="input-group">
                                <label asp-for="Password" class="control-label input-group-addon">
                                    <i class="material-icons">lock_outline</i>
                                </label>
                                <input asp-for="Password" class="form-control" placeholder="Password" />
                                <span asp-validation-for="Password" class="text-danger"></span>
                            </div>
                            <div class="input-group" style="color: #f18585">
                                <div class="checkbox">
                                    <label asp-for="RememberMe">
                                        <input asp-for="RememberMe" />
                                        @Html.DisplayNameFor(m => m.RememberMe)
                                    </label>
                                </div>
                            </div>
                        </div>
                        <div class="footer text-center">
                            <button type="submit" class="btn btn-lg custom-header">Log In</button>
                            <p>
                                <a asp-action="Register" asp-route-returnurl="@ViewData["ReturnUrl"]">Register a new account!</a>
                            </p>
                            <p>
                                <a asp-action="ForgotPassword">Forgot password?</a>
                            </p>
                        </div>
                    </form>
            </div>
        </div>
    </div>
</div>

</div>
    @section Scripts {
        @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
    }
